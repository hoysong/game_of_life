# Game of Life 렌더링 성능 측정

이 문서는 Game of Life를 개발하면서 렌더링 작업에 멀티스레딩을 단계적으로 적용하고 처리시간을 비교한 과정을 정리한 기록입니다.

이 측정은 정밀한 성능 벤치마크가 아닙니다. 당시 개발 환경에서 동일한 화면 크기와 종료 조건을 사용해 각 구현의 처리시간을 한 번씩 비교했습니다.

## 1. 측정 대상

한 프레임은 다음 순서로 처리됩니다.

1. Game of Life 규칙에 따라 다음 세대의 맵을 계산합니다.
2. 이미지의 배경을 채웁니다.
3. 가로·세로 그리드선을 그립니다.
4. 살아 있는 셀을 그립니다.
5. 완성된 이미지를 창에 출력합니다.

이번 비교에서는 **배경, 그리드선, 셀을 이미지에 그리는 렌더링 과정**에만 멀티스레딩을 적용했습니다. 다음 세대의 맵을 계산하는 과정은 순차 처리했습니다.

## 2. 측정 방법

프로그램 시작 시 `gettimeofday()`로 시작 시각을 밀리초 단위로 저장했습니다.

```c
long long get_call_time(void)
{
	struct timeval s_time;

	gettimeofday(&s_time, NULL);
	return (s_time.tv_sec * 1000 + s_time.tv_usec / 1000);
}
```

MiniLibX의 반복 루프에서 맵 갱신과 렌더링을 실행한 뒤, 같은 종료 조건에 도달하면 시작 시각과의 차이를 출력했습니다.

```c
map_modify();
fill_background();
put_background();
if (frame_count++ > 120)
{
	printf("%lld\n", get_call_time() - prog_start_time);
	exit(0);
}
```

당시에는 이를 120프레임 기준으로 기록했습니다. 다만 후위 증가 연산자를 사용하는 `frame_count++ > 120` 조건상 실제 반복 루프는 약 122회 실행됩니다. 모든 구현에 같은 종료 조건을 사용했으므로 상대적인 처리시간 비교에는 동일하게 적용됩니다.

## 3. 기록된 조건

| 항목 | 값 |
| --- | ---: |
| 맵 크기 | 274 × 142 cells |
| 창 크기 | 1918 × 994 pixels |
| `PIX_SIZE` | 7 |
| 초기 맵 | 인자 없이 실행하여 난수로 생성 |
| 시간 단위 | milliseconds |

각 스레드는 화면 높이를 기준으로 서로 다른 이미지 영역을 담당했습니다. 예를 들어 8개 스레드를 사용하면 화면의 세로 영역을 8개 청크로 나누고, 각 스레드가 할당된 범위에만 픽셀을 기록합니다.

## 4. 단계별 측정 결과

| 단계 | 실제 렌더링에 적용한 스레드 | 처리시간 | 단일 스레드 대비 단축 |
| --- | ---: | ---: | ---: |
| 멀티스레딩 적용 전 | 1 | 1,528ms | 기준 |
| 배경 렌더링 | 4 | 1,045ms | 약 31.6% |
| 배경 렌더링 | 8 | 934ms | 약 38.9% |
| 배경·그리드선 렌더링 | 8 | 704ms | 약 53.9% |
| 배경·그리드선·셀 렌더링 | 8 | 684ms | 약 55.24% |

단축률은 최초 단일 스레드 측정값인 1,528ms를 기준으로 계산했습니다.

```text
(1,528 - 684) / 1,528 × 100 ≈ 55.24%
```

## 5. 적용 과정

### 5.1. 배경 렌더링

가장 많은 픽셀을 기록하는 배경 처리부터 병렬화했습니다. 4개 스레드를 적용했을 때 1,045ms, 8개 스레드로 늘렸을 때 934ms가 기록됐습니다.

### 5.2. 그리드선 렌더링

8개 스레드를 유지하면서 가로·세로 그리드선 처리도 분할했습니다. 배경과 그리드선 렌더링을 함께 병렬화한 결과 704ms가 기록됐습니다.

### 5.3. 셀 렌더링

마지막으로 맵의 살아 있는 셀을 그리는 작업을 화면 영역별로 분할했습니다. 배경, 그리드선, 셀 렌더링에 모두 8개 스레드를 적용한 결과 684ms가 기록됐습니다.

## 6. 렌더링에 뮤텍스를 사용하지 않은 이유

이미지는 여러 스레드가 공유하지만, 각 스레드는 자신에게 할당된 높이 범위에만 픽셀을 기록합니다. 서로 같은 픽셀을 동시에 수정하지 않도록 영역을 분리했기 때문에 렌더링 단계에는 뮤텍스를 사용하지 않았습니다.

반면 다음 세대의 맵을 계산할 때는 한 셀의 주변 8칸을 함께 읽습니다. 스레드가 맵을 직접 수정하면 다른 스레드가 아직 계산 중인 값을 읽을 수 있으므로 별도의 동기화 또는 읽기·쓰기 버퍼 분리가 필요합니다. 이 실험에서는 해당 계산을 병렬화하지 않았습니다.

## 7. 측정의 한계

- 각 구성은 한 번씩 측정한 개발 단계 기록입니다.
- 초기 맵은 실행할 때마다 난수로 생성됐습니다.
- 반복 중 마우스 위치에 따라 살아 있는 셀이 추가될 수 있습니다.
- 측정 당시의 CPU, 운영체제, 컴파일러 버전은 기록하지 않았습니다.
- `gettimeofday()`로 전체 반복 구간을 측정했기 때문에 렌더링 외의 맵 갱신과 창 출력 시간도 포함됩니다.
- 따라서 절대적인 성능이나 다른 환경에서의 결과를 보장하지 않으며, 같은 프로젝트에서 렌더링 방식을 단계적으로 변경했을 때의 처리시간 차이를 보여주는 자료입니다.

## 8. 결론

배경, 그리드선, 셀 렌더링을 화면 영역별로 분할하고 8개 스레드로 처리한 결과, 당시 기록에서 전체 반복 구간의 처리시간이 1,528ms에서 684ms로 줄었습니다. 최초 구현과 비교한 단축률은 약 55.24%였습니다.
